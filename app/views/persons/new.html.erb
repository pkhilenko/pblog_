<div class="col-md-6 col-md-offset-3">
<h1>Add new person</h1>
    <%= form_tag(controller: 'persons', action: 'create', method: 'post') do   %>
      Введите email: <br />
      <%= email_field :email, class: 'form-control' %>
      <%= submit_tag "Save", class: "btn btn-primary" %>
    <% end %>
 </div>
